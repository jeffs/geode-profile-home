For <C-j> in Vim (my mapping to open code block):

* Automatically close open parens around the new block
* Do the right thing in shell scripts
  - `{}` for functions, `do....done` for loops, `then....fi` for if-statements
